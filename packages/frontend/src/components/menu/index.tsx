import React from 'react';
import { Container, Logo, Divisor } from './styles';

const Menu: React.FC = () => (
  <Container>
    <Logo />
    <Divisor />
    <Divisor />
  </Container>
);

export default Menu;
